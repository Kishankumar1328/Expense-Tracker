# FinSentinel - Architecture & Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     MOBILE APPLICATION                       │
│                    (React Native + Expo)                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Splash     │  │    Login     │  │   Signup     │      │
│  │   Screen     │→ │   Screen     │→ │   Screen     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           MAIN APP (Bottom Tab Navigation)          │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  Dashboard │ Expenses │ Insights │ Simulator │ Profile│  │
│  └─────────────────────────────────────────────────────┘    │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              CONTEXT PROVIDERS (State)              │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  AuthContext │ FinanceContext │ ThemeContext       │    │
│  └─────────────────────────────────────────────────────┘    │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                 REUSABLE COMPONENTS                  │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  Button │ Card │ Input │ LoadingState │ EmptyState │    │
│  └─────────────────────────────────────────────────────┘    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                    HTTP/REST API (Axios)
                            │
┌───────────────────────────┴─────────────────────────────────┐
│                      BACKEND SERVER                          │
│                  (Node.js + Express.js)                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   API ROUTES                         │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  /auth │ /expenses │ /budgets │ /insights │ /sims  │    │
│  └─────────────────────────────────────────────────────┘    │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                  MIDDLEWARE                          │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  JWT Auth │ CORS │ Error Handler │ Logger          │    │
│  └─────────────────────────────────────────────────────┘    │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                  CONTROLLERS                         │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  Auth │ Expense │ Budget │ Insight │ Simulation    │    │
│  └─────────────────────────────────────────────────────┘    │
│                            ↓                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   DATABASE                           │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  Users │ Expenses │ Budgets │ AI_Insights          │    │
│  └─────────────────────────────────────────────────────┘    │
│                      (SQLite)                                │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Authentication Flow
```
User → Login Screen → AuthContext.login()
  → POST /api/auth/login → authController.login()
    → Verify credentials → Generate JWT
      → Return token + user data
        → Store in AsyncStorage
          → Navigate to Dashboard
```

### Expense Management Flow
```
User → Expenses Screen → Add Expense
  → FinanceContext.addExpense()
    → POST /api/expenses → expenseController.addExpense()
      → Insert into database
        → Return expense data
          → Update local state
            → Refresh UI
```

### AI Insights Flow
```
User → Insights Screen → Load Insights
  → FinanceContext.fetchInsights()
    → GET /api/insights → insightController.getInsights()
      → Fetch user expenses
        → Analyze spending patterns
          → Generate insights
            → Store in database
              → Return insights
                → Display to user
```

### Financial Simulation Flow
```
User → Simulator Screen → Input parameters
  → FinanceContext.runSimulation()
    → POST /api/simulations → simulationController.runSimulation()
      → Calculate compound interest
        → Generate chart data
          → Return projection
            → Display results + chart
```

## 📊 Component Hierarchy

```
App
└── ThemeProvider
    └── AuthProvider
        └── FinanceProvider
            └── AppNavigator
                ├── SplashScreen
                ├── LoginScreen
                ├── SignupScreen
                └── MainTabs
                    ├── DashboardScreen
                    │   ├── Card (Financial Overview)
                    │   ├── Card (AI Insight)
                    │   ├── Card (Quick Actions)
                    │   ├── Card (Category Chart)
                    │   └── Card (Recent Transactions)
                    ├── ExpensesScreen
                    │   ├── Category Filters
                    │   ├── Expense List
                    │   └── Modal (Add/Edit Form)
                    ├── InsightsScreen
                    │   └── Insight Cards
                    ├── SimulatorScreen
                    │   ├── Type Selector
                    │   ├── Input Form
                    │   ├── Results Card
                    │   └── Chart
                    └── ProfileScreen
                        ├── User Info Card
                        ├── Settings Card
                        ├── Data Management Card
                        ├── About Card
                        └── Logout Button
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│         Client (Mobile App)             │
├─────────────────────────────────────────┤
│  1. User enters credentials             │
│  2. Send to backend                     │
│  3. Receive JWT token                   │
│  4. Store in AsyncStorage               │
│  5. Include in all API requests         │
└──────────────┬──────────────────────────┘
               │
               ↓ Authorization: Bearer <token>
┌──────────────┴──────────────────────────┐
│         Server (Backend API)            │
├─────────────────────────────────────────┤
│  1. Receive request with token          │
│  2. Auth middleware validates token     │
│  3. Extract user ID from token          │
│  4. Attach to request object            │
│  5. Controller uses user ID             │
│  6. Return user-specific data           │
└─────────────────────────────────────────┘
```

## 🎨 Theme System

```
ThemeContext
├── isDark (boolean)
├── toggleTheme (function)
└── colors (object)
    ├── background
    ├── surface
    ├── card
    ├── text
    ├── textSecondary
    ├── border
    ├── primary
    ├── success
    ├── warning
    └── danger
```

## 📱 Screen States

Each screen handles:
1. **Loading State** - Shows spinner while fetching data
2. **Empty State** - Shows message when no data exists
3. **Error State** - Shows error message on failure
4. **Success State** - Shows data with proper UI

## 🔄 State Management

### AuthContext
- User authentication state
- Login/Signup/Logout functions
- Token management
- Persistent sessions

### FinanceContext
- Expenses array
- Budgets array
- Insights array
- Summary object
- CRUD operations
- Data refresh

### ThemeContext
- Dark/Light mode toggle
- Color scheme
- Persistent theme preference

## 🚀 Deployment Ready

The application is structured for easy deployment:

### Frontend
- Can be built for Android APK
- Can be built for iOS IPA
- Can be deployed to Expo

### Backend
- Can be deployed to Heroku
- Can be deployed to AWS
- Can be deployed to DigitalOcean
- Can switch to PostgreSQL for production

---

**FinSentinel** - Professional architecture for a production-ready fintech app! 🏗️
